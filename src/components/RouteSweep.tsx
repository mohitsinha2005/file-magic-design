/**
 * Route change indicator removed intentionally — page transitions are handled
 * by the fade/lift in PageTransition for a calmer, more professional feel.
 */
const RouteSweep = () => null;

export default RouteSweep;
